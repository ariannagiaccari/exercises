const student = {
    info: { name: "Bob", age: 25 },
    grades: { math: 95, science: 89 }
  };
const {info, grades} = student 
const name = console.log(info.name);
const age = console.log(info.age);
console.log(grades);